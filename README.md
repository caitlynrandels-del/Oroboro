# Oroboro
A zero-dependency runtime that replaces document rendering with world instantiation. Oroboro treats language as a semantic coordinate system. By declaring &lt;!DOCTYPE Oroboro>, it weaponizes invalid HTML spec: code is no longer written to display a page, but to compile an infinite, living universe.
